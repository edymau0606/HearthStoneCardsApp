class User {
    constructor (userName, userPassword, userRole){
        this.userName = userName;
        this.userPassword = userPassword;
        this.userRole = userRole;
    }
}

module.exports = User;